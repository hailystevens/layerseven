name: Deploy API

on:
  push:
    branches: [ main ]
    paths:
      - 'api/**'
      - '.github/workflows/deploy-api.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Write SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VM_SSH_KEY }}" > key.pem
          chmod 600 key.pem
          ssh-keyscan -H ${{ secrets.VM_HOST }} >> ~/.ssh/known_hosts

      - name: Install rsync
        run: sudo apt-get update && sudo apt-get install -y rsync

      - name: Prepare remote directory
        run: ssh -i key.pem ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} 'mkdir -p /home/haily/api'

      - name: Sync files to VM
        run: rsync -avzr --delete -e "ssh -i key.pem" api/ ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }}:/home/haily/api/

      - name: Restart API
        run: ssh -i key.pem ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} 'sudo systemctl restart api && sudo systemctl is-active api'

      - name: Health check
        run: |
          code=$(curl -sS -o /dev/null -w "%{http_code}" https://api.layerseven.tech/health)
          echo "HTTP $code"
          test "$code" = "200"
